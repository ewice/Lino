import { MinutePipePipe } from './minute-pipe.pipe';

describe('MinutePipePipe', () => {
  it('create an instance', () => {
    const pipe = new MinutePipePipe();
    expect(pipe).toBeTruthy();
  });
});
